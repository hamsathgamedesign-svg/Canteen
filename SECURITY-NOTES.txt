McLEOD's English School Canteen Monitor - Security v2

Admin username: admin
Admin password: canteen123
Second security phrase: mcleods-secure-2026

The admin login now has two steps. The first step checks the username/password and the second checks the security phrase. Credentials are verified with PBKDF2-SHA-256 (310,000 iterations) and failed attempts are rate-limited.

Important: this is client-side application security. For strong protection of student/teacher data, Firebase Authentication and restrictive Realtime Database Security Rules should also be enabled so the database cannot be read or written directly without authentication.
